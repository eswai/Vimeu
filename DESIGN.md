# vimeu 設計ドキュメント

vimeu は macOS 向けのかな漢字変換 IME である。**Mozc の辞書と接続コスト行列をそのまま取り込み、
変換コストの計算を Mozc と同一にする**。前身の [shimei](../shimei) からは、変換方式ではなく
**成果物としてのコード**（サンドボックス構成・IMKit 回避策・ローマ字オートマトン・mmap 辞書の骨格・
TSV ユーザー辞書・調整ウィンドウ）を引き継いだ。

本書が設計の正本。

---

## 1. なぜ作り直したか

shimei は Mozc 由来の**単語コストだけ**を使い、品詞の連接コストを持たない PMI 共起方式だった。
shimei の DESIGN.md §3.4 は、その構造的限界を自ら記録していた:

> `となり→隣` のような誤りは残る。これは品詞の連接コストを持たないことに起因する構造的な限界で、
> 共起辞書の追加では埋まらない。**次にやるなら**: Mozc の `id.def` +
> `connection_single_column.txt` を辞書に取り込み、トークンに lid/rid を持たせて Viterbi に
> 遷移コストを足す。

vimeu はその「次」である。実測の結果は明快だった:

| | Wikipedia (13,804文) | 会話文 (11,542文) |
|---|---|---|
| shimei 文字精度 | 70.89% | 72.91% |
| **vimeu 文字精度** | **84.47%** | **89.44%** |
| shimei 文精度 | 27.27% | 16.43% |
| **vimeu 文精度** | **34.63%** | **40.76%** |
| shimei top-5 | 34.53% | 28.82% |
| **vimeu top-5** | **46.72%** | **54.66%** |

**同一のテストセット**での比較である。しかも vimeu は Mozc UT 辞書 490 万行を**落として**いる
（理由は §2.1）。辞書を 1/5 に減らしてなお +13.6pt / +16.5pt 改善した。
接続コストが持っていた情報量は、辞書規模や共起統計で代替できるものではなかった。

### 1.1 shimei からの変更点と理由

| 項目 | shimei | vimeu | 理由 |
|---|---|---|---|
| 変換方式 | PMI 共起方式（ラティス + ビーム探索、文全体スコア） | **Mozc の Viterbi（単語コスト + 接続コスト）** | 上表 |
| 辞書のコスト | Mozc コストを ±4 のスコアへ写像し、短語減点を加算 | **Mozc のコストをそのまま** | 写像も短語減点も、接続コストが無いことの代償だった。接続コストが入れば不要 |
| トークンのキー | (読み, 表層) | **(読み, 表層, lid, rid)** | Mozc では同じ表記が複数の品詞で登録され、接続の仕方が違う。畳むと接続コストが消える |
| 共起辞書 / PMI | コーパスから抽出した PMI をスコアの一部にする | **ユーザーが手で定義するペア表**（§3.5） | 接続コストの代わりに使うと負ける。上乗せなら効く。抽出をやめて手書きにしたのは、誤ったペアが既存の正解を壊したときに原因が見えないから |
| 調整の係数 (α/γ/minPMI) | あり | **なし** | Mozc のコスト計算に自由パラメータは無い。共起もブール判定なので閾値も重みも持たない |
| ユーザー辞書の値 | score ∈ [−4, 4]、大きいほど優先 | **cost ∈ [0, 32767]、小さいほど優先** | Mozc と同じ土俵に乗せる |
| 中間 TSV | `word_dict.tsv` を経由 | **なし。Mozc の `src/data` を直読み** | 写像が恒等なので、中間形は原ファイルの遅い複製にしかならない |
| 辞書サイズ | 112MB | **52MB** | UT 辞書を落としたぶん縮み、接続行列 14MB を足してなお半分以下 |

### 1.2 継承しないもの

Mozc から取り込むのは**辞書データとコスト計算だけ**である。次のものは移植していない:

- `rewriter/` 一式（日付変換・数値変換・記号・絵文字・単漢字・変体仮名など）。
  ただし `collocation_rewriter` の**考え方**だけは §3.5 に取り込んだ。データ自体は移植しようがない
  — OSS の `collocation.txt` は 40 行しかなく、本体は Google 内部にある
- `predictor/`（予測変換）と学習（`user_history_predictor` / `segment_history_rewriter`）
- `Segmenter` の文節境界ビット配列と、それに基づく文節単位の変換 UI
- `KeyCorrector` / `InsertCorrectedNodes`（打ち間違いの補正）
- `ResegmentArabicNumberAndSuffix` などの再分割規則
- `NBestGenerator` の `CandidateFilter` と `structure_cost`
- history segment（直前の確定文脈をラティスに載せる仕組み）

### 1.3 可搬性

`VimeuEngine` / `VimeuDict` / `VimeuInput` は Foundation にしか依存しない。
OS 依存は `VimeuUI` / `VimeuIME` に閉じる。

この分離には実利がある。**IME プロセスはブレークポイントでデバッグできない**
（デスクトップごと固まる）ため、ライブラリとしての単体テストと CLI が唯一の実用的な検証手段になる。

```
┌──────────────────────────────────────────────────┐
│ VimeuIME.app  （プロセスは1つだけ）                 │
│                                                  │
│  VimeuIME      IMKInputController / 状態遷移       │  macOS 依存
│  VimeuUI       候補パネル・調整ウィンドウ            │  macOS 依存
│  ────────────────────────────────────────────    │
│  VimeuInput    ローマ字オートマトン・入力バッファ     │  Foundation のみ
│  VimeuEngine   ラティス + Viterbi + A* + オーバレイ  │  Foundation のみ
│  VimeuUserDict ユーザー編集（値型と TSV ストア）      │  Foundation のみ
│  VimeuDict     vimeu.dic の writer / mmap reader   │  Foundation のみ
└──────────────────────────────────────────────────┘
        ▲                          ▲
        │                          │
  vimeu-dictbuild   vimeu-eval     vimeu-preview
  （辞書ビルド CLI） （精度回帰 CLI） （調整 UI の開発用ハーネス）
```

---

## 2. 辞書

### 2.1 Mozc の `src/data` をそのまま入力にする

必要なものはすべて Mozc のソースツリーにプレーンテキストで入っている。**中間形式は作らない。**

```
<MOZC_DATA>/dictionary_oss/dictionary00..09.txt   読み \t lid \t rid \t コスト \t 表層
<MOZC_DATA>/dictionary_oss/id.def                 "<id> <品詞feature>" × 2672
<MOZC_DATA>/dictionary_oss/connection_single_column.txt
                                                  先頭行 2672、以降 2672² 個のコスト（行優先 [rid][lid]）
<MOZC_DATA>/rules/boundary.def                    PREFIX/SUFFIX <品詞パターン> <コスト>
    │  vimeu-dictbuild pack --mozc-data <MOZC_DATA> --out dict/vimeu.dic
    ▼
dict/vimeu.dic                                    単一バイナリ 52.3MB
```

ビルダは `<MOZC_DATA>` 直下の `dictionary_oss/` と `rules/` を **Mozc のディレクトリ構造のまま**探す。
`cp -R mozc/src/data dict/mozc` で終わり、前処理は要らない。
`connection_single_column.txt` の先頭行と `id.def` の行数が食い違えば、そこで止める
（別バージョンの混在の検出）。

インストール済み Mozc.app の**ビルド済み**辞書は使わない。macOS 版はデータセットを
`MozcConverter` 実行ファイルに埋め込んでいて（`Contents/Resources/` に `.data` が無い）、
取り出せたとしても LOUDS トライと圧縮接続表のデコーダを移植する必要がある。テキスト原本のほうが安い。

#### なぜ Mozc UT 辞書を使わないか

`mozcdic-ut-*` は 490 万行あり、shimei の網羅性を支えていた。だが**全行が `lid = rid = 0000`** で、
0 は `id.def` では BOS/EOS の品詞 ID である。接続コストを使う設計では、これらの語は
「文頭・文末としか繋がらない」ものとして扱われ、まともに変換に載らない。

既定の品詞 ID を一律に割り当てて取り込むことはできる（`--extra <glob> --extra-pos <id>`）が、
490 万語すべてが同一品詞として振る舞うため接続コストの精度は落ちる。既定では無効。

#### フィルタ規則と実測値

- 読みがかな以外を含む行は落とす（かな入力から到達不能）。129 万行中 3,415 行
- 読みが 17 かな以上の行は落とす（ラティスが引かない）。4,961 行
- 同一 `(読み, 表層, lid, rid)` が複数回出たら最小コストを採る
- 結果: **トークン 1,215,479 / 読み 739,398 / 表層 978,579 / 品詞 2,672**

`boundary.def` の展開は Mozc の `converter/gen_boundary_data.py` の移植。パターンの `*` を `[^,]+` に、
末尾が `,` でなければ `(?:,|$)` を足して `^` 固定で `id.def` の feature に先頭一致させ、
最初にマッチした行のコストを採る。マッチ無しは 0。実測で prefix 1,459 / suffix 317 品詞に付く。

### 2.2 `vimeu.dic` フォーマット

単一ファイル、リトルエンディアン、全セクション 8 バイト境界整列。実行時は `mmap(2)`
（`MAP_PRIVATE` + `madvise(MADV_RANDOM)`）で写像し、**起動時のパースは行わない**
（例外は約 90 文字のかなアルファベットと 2,672 件の品詞名インデックスだけ）。

| セクション | 内容 |
|---|---|
| Header (192B) | magic `VIMEUDIC`、フォーマット版、件数、`posCount`、`unknownPOSID`、チェックサム、セクション表 9 件 |
| Alphabet | 読みに現れる Unicode スカラ（昇順） → 1 バイトのアルファベット ID |
| KeyOffsets / KeyBytes | 全読みをアルファベット ID 列にして**ソート**して連結したもの |
| TokenOffsets / Tokens | `surfaceRef: UInt32` + `cost: Int16` + `lid: UInt16` + `rid: UInt16` + 予約 `UInt16` の **12B/件**。読みごと cost 昇順 |
| SurfacePool | `[len: UInt16][UTF-8]` の連結。表層形は重複排除 |
| **Connection** | `Int16` × posCount²（14.3MB）。`[rid * posCount + lid]`、Mozc の行列そのまま |
| **Boundary** | (prefix: `UInt16`, suffix: `UInt16`) × posCount |
| **PosNames** | `[len: UInt16][UTF-8]` × posCount。`id.def` の feature 文字列 |

**なぜソート済みキー表か。** ラティス構築がやりたいのは各開始位置からの共通接頭辞検索である。
キーをソートしておくと、同じ接頭辞を持つキーは連続した 1 区間に固まり、その中で接頭辞そのものに
等しいキーが先頭に来る。つまりキー表がそのままトライとして働き、1 かな進めるごとに二分探索 2 回で
区間を狭めるだけで済む。ダブル配列トライも同じ操作を提供するが、実測でこちらのほうが小さく、
構築コードが桁違いに短い。

**なぜ接続行列を非圧縮で持つか。** Mozc の実行時は簡潔ビットベクトルで圧縮した接続表を引き、
その遅さを補うためにキャッシュを噛ませている。vimeu は 14.3MB を素の `Int16` 配列として写像するので、
`trans(rid, lid)` は乗算 1 回と 2 バイト読みだけになる。mmap なので実際に触れたページしか常駐しない。
辞書全体を 52MB に収めたうえでこれができるのは、UT 辞書を落としたからでもある。

**なぜ品詞名まで入れるか。** 調整ウィンドウが「なぜこの候補が勝ったか」を品詞名で説明する。
副ファイルを増やすと、辞書を差し替えたときに版がずれる。150KB なら同梱したほうがよい。

**同点の解決は仕様である。** コストが並んだトークンは**表層形の UTF-8 順**、それも並べば lid/rid 順で並べる。
Mozc のコストは整数なので同点は頻繁に起こり、しかも最頻出語に集中する。どちらが先に並ぶかが、
コストだけでは決まらないすべての経路の勝者を決めてしまう。UTF-8 順はひらがな（U+3040–309F）を
カタカナ（U+30A0–30FF）より前に置くので、`を` が `ヲ` に、`の` が `ノ` に勝つ。

**サイズ**: 出荷辞書で **52.3MB**。

### 2.3 ユーザー辞書

vimeu は**自動学習を持たない**。確定しても辞書は一切書き変わらず、「この変換を優先したい」は
すべてユーザーの明示的な編集で表現する。

**保存形式は TSV**（`VimeuUserDict`）。SQLite ではない。編集は明示操作のときだけ発生し、規模は
数百〜数千行にとどまるので、トランザクションや差分更新より「人手で読めて diff でき、バックアップや
端末間の持ち運びがファイルコピーで済む」ほうが価値が高い。libsqlite3 を引かないので
`VimeuEngine` の可搬性も保てる。

サンドボックス下なので `FileManager.urls(for: .applicationSupportDirectory, ...)` は
**アプリコンテナ内**を返す（entitlement 不要）:

```
<container>/Library/Application Support/VimeuIME/
  user_word.tsv          reading \t surface \t cost \t disabled \t updated_at
  user_collocation.tsv   left \t right \t polarity \t updated_at         （§3.5）
  user_connection.tsv    left_pos \t right_pos \t cost \t disabled \t updated_at
                                                                          （§4.2）
```

3 つのファイルは独立に書き換わる。単語の編集・ペアの登録・接続の編集は UI 上の別の操作で、
1 つの保存が他を巻き添えにしないほうがよい。

`cost` が空欄は「値なし（無効化のみ）」。出力は常にソート済みで決定的。書き込みは一時ファイル＋
`rename` のアトミック全書き換え。

#### 合成規則

| ユーザー行 | `vimeu.dic` にある | 有効辞書での結果 |
|---|---|---|
| なし | ○ | システム値 |
| `cost` あり・`disabled=0` | ○ | 上書き |
| `cost` あり・`disabled=0` | ✗ | ユーザー追加語 |
| `disabled=1` | ○ | 除外（打消し線＋「復活」） |
| `disabled=1` | ✗ | 何もしない |

**由来判定は「`vimeu.dic` に同じキーがあるか」だけ**で、編集は元値を一切記録しない。だから
システム辞書を作り直して差し替えても、編集は 1 つも無効にならない（`baseCost` の表示だけが
新しい値に追随する）。

#### 編集のキーは (読み, 表層) である — トークンのキーではない

パックされた辞書はトークンを `(読み, 表層, lid, rid)` で持つが、**ユーザー編集は
`(読み, 表層)` で持つ**。Mozc は同じ表記を複数の品詞で登録しており、語を強めるのに
`名詞,固有名詞,人名,姓` と `名詞,一般` のどちらかを選ばせる UI は、誰も欲しがらない。
上書きはその (読み, 表層) を持つ**全トークンに一律**に適用し、削除は全トークンを隠す。

ユーザー追加語の lid/rid は Mozc の `unknown_id`（名詞,サ変接続）を使う。
Mozc が「辞書で説明できない語」に与える品詞であり、ユーザー追加語はまさにそれである。
この ID は `id.def` から解決して辞書ヘッダに書く（ハードコードしない）。

#### 接続の編集のキーは品詞 ID ではなく品詞名である

`user_connection.tsv` が持つのは `1583 → 261` ではなく
`BOS/EOS,*,*,*,*,*,* → 動詞,自立,*,*,五段・ワ行促音便,連用形,*` である。行列は `id.def` の
行番号で引くのに、ファイルには名前を書き、読み込み時に現在の辞書に対して解決する。

**ID は Mozc のデータ更新で動くから**である。上流が品詞を 1 つ挿入すれば以降の全 ID がずれ、
`1583 → 261` は別の品詞の組に静かに適用される。これは単語編集がキーを `(読み, 表層)` に
していることと同じ理由で、**編集はシステム辞書の内部表現ではなく、ユーザーが見たものを指す**。
名前は接続ペインに出ている文字列そのものなので、ファイルの中身と画面の内容が一致する。

現在の `id.def` に無い名前は有効行列から落ちるが**ファイルには残る**。読みが辞書から消えた
単語編集と同じ扱いで、品詞が戻れば編集も戻る。

#### 合成規則（接続）

| ユーザー行 | 有効行列での結果 |
|---|---|
| なし | Mozc の値 |
| `cost` あり・`disabled=0` | 上書き |
| `disabled=1` | 32767（`kMaxCost`）に固定。「復活」で戻せる |

`disabled` は無限大**ではない**。禁止した接続も通れなければならない——素通しノードが
必ず張るのと同じ理由で、ラティスは入力全体を張り続ける必要がある。ただ、他のどの経路にも負ける。

#### mmap との両立

システム辞書は不変ファイルの写像であり、十数件を変えるために 121 万件をヒープへ複製するのは
mmap 設計が避けようとしていることそのものである。そこで `OverlaidDictionary` は編集を小さな
脇テーブルに置き、探索が読み進む途中で合成する。

- **上書き・無効化**: 走査中のヒットごとに引く。ただし `WordKey` を作るには読みを
  アルファベットコードから復元する必要があり、全ヒットでやると 1 件ごとに文字列確保が要る。
  そこで**編集された表層形の集合**を先に引き、ほぼすべてのヒットを「Set の空振り 1 回・確保なし」
  で通す。復元は当たったときだけ。
- **ユーザー追加語**: 写像されたトライには載っていないので、読み長ごとの表を別に持ち、
  その長さの部分文字列だけを引く（追加語が無ければ何もしない）。

編集のたびに `OverlaidDictionary` を作り直して差し替えるので、バックグラウンドで走行中の変換は
一貫したスナップショットを読み続ける。

#### 値域と刻み

`cost ∈ [0, 32767]`。**クランプはコアが権威**（UI はソフト制限のみ）。
「強める / 弱める」の刻みは **∓500**。Mozc のコストは対数確率を 500 倍したもの
（Mozc 自身の `kCostDiff = 3453` が `500·log(1000)`）なので、500 は 1 *e* 倍にあたる。
体感できて、かつ同じ桁の接続コストを押し流さない大きさである。

「強める」は相対指定だが、**書込み時に絶対値へ解決**する — 現在の有効値 ∓500 をクランプして
保存するので、あとでシステム辞書を作り直しても、目で合わせた編集が動かない。

**値域も刻みも接続コストと共通**である。単語と接続は同じ単位の同じ桁の量で、片方だけ別の刻みに
する理由がない。実際 `したい` の例（§4.2）は 1 回の「強める」で順位が入れ替わる。

#### mmap との両立（接続）

接続の編集も脇テーブルである。`transitionCost` はデコーダで最も多く呼ばれる関数
（1 文あたり 10⁵ オーダー）なので、1 件でも編集があると毎回ハッシュを引く実装では
**測って 36% 遅くなった**（`corpus/testset.tsv` 13,804 文、release、Apple M4、3 回の中央値で
3,625 → 2,310 文/秒）。行（rid）ごとの `Bool` 配列 2,672 バイトを前に置き、
編集のある行だけがハッシュに落ちるようにすると **3,486 文/秒**、実質無料に戻る。

---

## 3. 変換アルゴリズム

Mozc の `converter/immutable_converter.cc` の忠実な移植（の、コスト計算部分だけ）。

### 3.1 ラティス

開始位置ごとに読み長 1..16 の部分文字列を辞書に引き、ヒットしたものをノードにする。
加えて**全位置に 1 かなの素通しノードを置く** — Mozc が `AddCharacterTypeBasedNodes` を
無条件に呼ぶのと同じで、これがラティスが必ず入力全体を張ることを保証する。
素通しノードのコストは Mozc と同じ `kMaxCost = 32767`、品詞は `unknown_id`。

`boundary.def` の prefix/suffix ペナルティは、**先頭で始まるノードと末尾で終わるノードの
`wcost` に畳み込む**（Mozc の `ApplyPrefixSuffixPenalty` と同じ）。位置に依存する量であって
遷移の量ではないので、ここで足しておけば Viterbi の内側ループは加算 1 回で済む。

### 3.2 Viterbi（前向き）

```
cost(BOS)  = 0
cost(node) = min over lnode ( cost(lnode) + trans(lnode.rid, node.lid) ) + node.wcost
cost(EOS)  = min over lnode ( cost(lnode) + trans(lnode.rid, 0) )
```

BOS も EOS も品詞 ID は 0（`id.def` のとおり）。ビームも枝刈りも無い — ラティスは厳密な
動的計画法で足りる大きさで、Mozc もそうしている。

接続表の引き方は Mozc の `CachingConnector` と同じ形にしてある。内側ループでは `node.lid` が固定で
`left.rid` だけが動くので、その行を rid でメモ化する。ただし無効化は世代スタンプで行う
（lid はノードごとに変わるので、2,672 要素の配列を毎回ゼロ埋めすると引く回数より高くつく）。

### 3.3 n-best（後ろ向き A\*）

前向き Viterbi が各ノードについて「BOS からそのノードの末尾までの最小コスト」を確定させている。
これは、そのノードにぶら下がる任意の後続部分に対する**厳密な**補完コストなので、A\* の
ヒューリスティックとして完全である。

```
f = viterbi.cost[node] − node.wcost + g(そのノードの先頭から EOS までの、構築済みの後続部分)
```

の順に優先度付きキューを取り出すと、経路は総コストの厳密な昇順で出てくる。したがって最初の
`limit` 件が真の k-best になる。Mozc の `NBestGenerator`（`fx = lnode->cost + gx`）と同じ形で、
文節機構（`structure_cost`・候補フィルタ・境界判定）だけを外したものである。

安全弁として展開回数に上限を置く（Mozc も `kMaxNodesSize` で同じことをする）。同一表層の候補は畳む。

**厳密性はテストで固定してある** — 小さなラティスに対して全経路を列挙する独立実装を書き、
A\* の出力と一致することを確認している（`ConverterTests.testNBestMatchesExhaustiveSearch`）。

### 3.4 候補のコスト内訳

```
cost = wordCost + connectionCost + collocationCost
  wordCost        = Σ node.wcost（boundary ペナルティ込み）
  connectionCost  = Σ trans(...)（BOS/EOS の遷移を含む）
  collocationCost = −3453 × 満たした正のペア数（§3.5。ペアを登録するまで常に 0）
```

この恒等式は厳密に成立し、テストで固定してある。調整ウィンドウはこの数字を並べて出すので、
足し算が合っていないと読めない。`collocationCost` だけは Mozc 由来ではないので、
単語コストに畳み込まず独立の項として持つ — 畳み込むと「なぜこの候補が勝ったか」が
辞書の値の中に消えてしまう。

負の共起に該当する候補はこの内訳を表示する前に除外するため、負の共起のためのコスト項はない。

### 3.5 共起 — ユーザーが定義する語のペア

Mozc の 2 項は、語と語の結びつきに対して原理的に盲目である。`かわをむく` の候補は
`川を向く` / `皮を向く` / `皮を剥く` のいずれも品詞列が 名詞 → 助詞 → 動詞 で完全に同一なので、
**接続コストは全候補で同じ 4514** になり、順位は `川` と `皮` の単語コスト差 15 だけで決まる。
出荷辞書では `皮を剥く` は 10 位、トップとの差は 964 である。辞書のどこにも
「剥かれるのは*皮*だ」とは書かれていない。

そこで第 3 項を置く。設計はすべて「小さく保つ」方向に倒してある。

**ブール判定であって、スコアではない。** ペアは登録されているかいないかの 2 値で、重みも閾値も
係数も無い。§1.1 の「自由パラメータは無い」が、この機能を足しても生き残るのはこのためである。
PMI を連続値として引く案は検討して捨てた — 単位としては
`−500·log p(y|x) = −500·log p(y) − 500·PMI` で整合するが、Mozc の `wcost` は純粋な unigram
ではなく、接続コストが品詞レベルで共起の一部を既に食っているので、係数 1 では二重計上になる。
係数を実測で決めるということは、自由パラメータを 1 つ増やすということである。

**コーパスから抽出せず、ユーザーが手で定義する。** 手書きのペアは*効かない*ことしかできないが、
抽出したペアは*誤って効く*ことができ、しかもその因果はユーザーからも開発者からも見えない。
接続コストの編集を 1 セルに閉じ、影響を測れる形でしか出さない理由（§4.2）と同じ構図である。
網羅性は最初から諦めていて、
足りない箇所は現状のまま — つまり単調な改善にしかならない。

**Viterbi の中ではなく、n-best の再ランクで効かせる。** ペアの 2 語は隣接しない（`皮 を 剥く`）。
ラティスで採点するには探索状態に「直前の自立語」を持たせることになり、デコーダが乗っている
マルコフ性と、§3.3 の A\* ヒューリスティックの許容性の両方が壊れる。
出来上がった一覧を並べ替えるだけなら、デコーダは 1 行も変わらない。

**窓で縛る。** 補正が効くのはトップ候補から `kCostDiff = 3453` 以内の候補だけで、ボーナスも
同じ 3453 である。Mozc の `CollocationRewriter` が `move_candidate(i, 0)` に課しているのと
同じ窓で、意味は「トップより 1000 倍以上ありえない候補は、ペア表が何を言おうと繰り上がらない」。
窓とボーナスを同じ値にしてあるので、**ここにも決めるべき数値は無い**。
複数ヒットは加算されるので、より多くのペアを満たす候補が上に来る — 2 つの候補がどちらも
当たったときの順序規則はこれだけで足り、ペアごとの重みは要らない。

**負の共起は別の意味で、同じ表に置く。** `CollocationEdit.polarity` は `positive` / `negative` の
2 値で、`user_collocation.tsv` の `polarity` 列に保存する。旧来の3列形式を読むときは
`positive` と解釈するので、既存のユーザー辞書は失われない。UI は正負を同じ「共起」タブで選び、
登録済みの行にも種別を表示する。

正の共起は上記の通り 3453 の割引で再ランキングする。一方、負の共起は「この2語は同じ候補に
絶対に現れない」というハード制約であり、コスト値を持たない。選択肢は正の共起と同じ自立語だが、
完成した候補の表層列にその2語が同時に現れれば、途中の助詞・未知語ノードを含めて語順を問わず
一致させる。A\* が完成させた経路に禁止ペアがあればその経路を採用せず、
許可された経路が必要数に達するまで探索を続ける。したがって、禁止された経路が Viterbi の最良
経路でも、正の共起の割引で順位を下げるだけの実装にはしない。

正負が同じ2語に対して同時に登録された場合は矛盾するため、後から登録した関係が前の関係を
置き換える。負の共起は順序なしなので、`(A, B)` と `(B, A)` は同じ編集である。正の共起の
`(A, B)` と `(B, A)` は従来どおり別の編集である。

**ペアは自立語どうし、順序あり。** `皮 を 剥く` の `を` は経路の語ではあってもペアの語ではない。
Mozc は文節を単位にすることで同じところに到達しているが、vimeu は文節を持たない（§3.7）ので、
`id.def` の feature を見て 助詞・助動詞・記号・接頭詞・非自立・接尾 を飛ばす形で同じ組を作る。
`(皮, 剥く)` と `(剥く, 皮)` は別のペアである。

ここでいう「順序あり」は正の共起に限る。負の共起は上記の通り順序なしで、同じ2語が文中の
どちら向きに現れても除外する。

**探索の深さ。** ペアが 1 つでも登録されていると n-best を最大 32 件まで掘る（`皮を剥く` は
10 位なので、9 件では届かない）。ただし打ち切りは件数ではなく**コスト**で、上の窓を超えた
時点で止める — 再ランクが届かない候補を作っても捨てるだけだからである。
ペアが 0 件のときは深さも打ち切りも一切変わらない。

代償は速度で、ペアを 1 つでも登録すると Wikipedia テストセットで
3,780 文/秒 → 1,240 文/秒（1 文 0.8ms）になる。深く掘るぶんそのままである。
ライブ変換は打鍵ごとに短い読みを変換するので体感には出ないが、無料ではない。

### 3.6 精度

§1 の表のとおり。加えて **Mozc 自身の回帰データ**での忠実度:

| corpus | 文数 | 文字精度 | 文精度 | top-5 |
|---|---|---|---|---|
| `corpus/testset.tsv`（Wikipedia） | 13,804 | 84.47% | 34.63% | 46.72% |
| `corpus/testset_messenger.tsv`（会話文） | 11,542 | 89.44% | 40.76% | 54.66% |
| `corpus/mozc_evaluation.tsv` | 429 | 97.66% | **91.14%** | 96.04% |

`mozc_evaluation.tsv` は Mozc の `data/dictionary_oss/evaluation.tsv` のうち、**Mozc 自身が
`OK:` と記録している 429 件**である（`FAILED:` の 157 件は Mozc も外すので除いた）。
ここでの文精度 91.14% が、Mozc への忠実度そのものと言ってよい。残り 9% は
rewriter・segmenter・学習といった、意図的に継承しなかった層が効いている部分である。

速度は Wikipedia テストセットで 3,800 文/秒（release ビルド、M シリーズ）。
共起ペアを登録すると n-best を深く掘るので 1,240 文/秒に落ちる（§3.5）。

### 3.7 既知の限界

- **文節境界を操作できない**。文全体の n-best しか出さないので、「ここで区切りたい」は表現できない。
  Mozc の `Segmenter` を移植すれば可能になるが、辞書・デコーダ・UI の全層に及ぶ
- **学習しない**。同じ誤変換は明示的に編集するまで繰り返す（それが設計方針である。§2.3）
- **記号・数字・英字の変換を持たない**。読みはかなだけを受け付ける
- **接続コストの編集の影響を、その場では見せられない**。編集は品詞の組に効くので、
  効果はテストセットを掛け直さないと分からない（§4.2）

---

## 4. macOS UI

### 4.1 状態遷移

```
Direct ──かな入力──▶ Composing ──Space──▶ Converting
  ▲                    │                     │
  └──Enter/Esc─────────┘                     │
  └──────────────Enter（確定）────────────────┘
                       ◀──────Esc（読みは保持）─┘
```

- **Direct**: マークドテキストなし
- **Composing**: 読みバッファを編集。ライブ変換が有効なら毎キーストロークで変換し、最良候補をインライン表示
- **Converting**: 文全体 n-best を候補パネルに並べ、↑↓ / 数字キーで選び Enter で確定。
  候補は先頭 9 件などで切らず、n-best 探索の安全弁（展開 20,000 回）までに列挙できた
  全候補を渡す。パネルは選択位置を含む 10 件単位でページングし、↑↓ / Space で
  ページ境界を越えたときに前後のページへ切り替える。
  20,000 は `NBest.search` の展開上限に対してコードとテストで検証する値であり、候補件数の保証値ではない。

Esc は**状態を 1 枚ずつ巻き戻す**キーであり、「入力を消す」キーではない
（Converting → Composing では読みを保持する）。

### 4.2 調整ウィンドウ

入力メニューの「調整...」で開く。開いた時点で**編集中の読みが入った状態**になるので、
おかしな変換に出会った文脈のまま調整に入れる。

**ペインの切替はウィンドウの統合ツールバー**（`toolbarStyle = .unified`、中央に
`NSSegmentedControl`）に置く。カレンダーの 日/週/月/年、Finder の表示切替と同じ位置で、
これがウィンドウを「普通の macOS のウィンドウ」に見せる要になる。実利もあって、切替 UI が
コンテンツから外れるので、**下のペインの高さが変わっても読みの入力欄が動かない**。

**読みと候補は常に上部に固定**する。編集の良し悪しは常に候補の並びで判断するので、
候補だけはどのペインでも見えている必要がある。配色・字送り・コントロールはすべてシステム標準
（`Table` / `Form(.grouped)` / セマンティックカラー）に任せ、ポイント数を直接指定しない。
ライト／ダーク双方で確認済み。

- **候補**（常時表示）: n-best を**分割した形だけ**で出す。分割の各要素はそのまま下の「単語」ペインの
  行＝編集できる辞書エントリなので、素の文字列を併記しても同じことを二度言うだけになる。
  各行に **合計 / 単語 / 接続** のコストを添え、`合計 = 単語 + 接続` が厳密に成立する。
  ペアが 1 つでも効いている文では **共起** の列が加わり、`合計 = 単語 + 接続 + 共起` になる。
  効いていない間は列ごと出さない — 使っていない機能の説明として 0 が並ぶだけになるからである。
  候補行は遅延生成する。実辞書の `みなみ` は `NBest.search` の展開上限 20,000 回に対して
  重複除去後 4,093 候補（2026-08-21、`vimeu-eval --convert みなみ --topn 20000` で検証）となり、
  全行の一括生成は調整ウィンドウの操作を妨げるためである。共起列の有無も描画ごとに一度だけ集計する。
  **行をクリックすると「接続」ペインの説明対象になる** — 遷移は 1 つの経路の中でしか意味を持たない
- **共起**（§3.5）: 種別（**正の共起 / 負の共起**）と、自立語を選ぶ**ドロップダウン 2 つ**で登録する。
  正の共起は一緒に選ばれるようになり、負の共起は同じ候補から除外される。下に登録済みのペアが
  種別つきで並び、正の共起でいま画面に出ている候補に当たっているものには「有効」が付く。
  選択肢は**現在の全候補の自立語**を集めたもので、テキスト入力ではない。
  入力欄にすると打ち間違い・ラティスに出ない活用形・別の文の語が入り、**絶対に発火しないペア**を
  登録できてしまう。そして `皮` は 2 位、`剥く` は 9 位の候補にあるので、
  選択肢は**選択中の候補ではなく全候補**から採らなければ、登録したいペアがそもそも組めない。
  助詞は選択肢に出さない（`皮 を 剥く` のペアは 皮 × 剥く である）
- **単語**: 候補が使ったエントリと、無効化中で読みが文中に現れるエントリを **cost 昇順**で。
  操作は 強める / 弱める / 数値直接編集 / 削除（復活）/ リセット。
  一覧は**表記**と**品詞**を出す。品詞を出すのは、Mozc のエントリは品詞で意味が変わるからで、
  これが無いと同じ表記の 2 行が区別できない。読みは、同じ表記が複数行に出るときだけ添える
- **接続**: 選択中の候補の各境界を `左語 (品詞) → 右語 (品詞) コスト` で並べる。
  BOS/EOS の遷移も含める。ここが「なぜこの候補が勝ったか」を説明する唯一の場所であり、
  操作は単語ペインと同じ 5 つ（強める / 弱める / 数値直接編集 / 使わない（復活）/ リセット）。
  ただしフッタが**この編集は同じ品詞の組を通るすべての文に効く**と述べる

#### 接続コストの編集

**単語コストの編集は局所的だが、接続コストの編集は大域的である。** `隣` を弱めるのは
`隣` が出る文にしか効かないが、`BOS → 動詞,自立,連用形` を強めるのは**日本語のすべての文**に効く。
しかも 2672² = 714 万ペアあるので、一覧して確かめることもできない。この非対称性は消えていない。

それでも編集を出すのは、**接続コストでしか表現できない誤変換があるから**である。`したい` を引くと:

| 候補 | 単語 | 接続 | 合計 |
|---|---|---|---|
| 死体 (名詞,一般) | 5160 | BOS→名詞 **1066** | 6226 |
| したい (動詞,自立,連用形) | 3219 | BOS→動詞連用形 **3132** | 6351 |

単語コストは `したい` のほうが 1941 も安い。負けているのは**文が連用形で始まること**に対する
罰であって、語の重みではない。ここで `死体` を弱めれば `死体` が出るべき文まで壊れる。
直すべき対象は語ではなく、`BOS → 動詞,自立,連用形` という 1 つの遷移である。
実際「強める」1 回（3132 → 2632）で `したい` が 1 位になる。

**大域的な影響は、測れるようにしてある。** `vimeu-eval --user <ユーザー辞書のフォルダ>` は
IME とまったく同じ有効辞書で走るので、編集の前後をテストセット上で直接比べられる。
上の 1 件を入れた実測（3 テストセット全件）:

| | 文字精度 | 文精度 | top-5 |
|---|---|---|---|
| Wikipedia 13,804文 | 84.47% → 84.47% | 34.63% → 34.63% | 46.72% → 46.72% |
| 会話文 11,542文 | 89.44% → 89.44% | 40.76% → 40.76% | 54.66% → **54.65%** |
| `mozc_evaluation` 429文 | 97.66% → 97.66% | 91.14% → 91.14% | 96.04% → 96.04% |

top-5 の 1 文だけが動いた。これが「見えている 1 文のために、見えていない 25,775 文に何をしたか」の
答えであり、この数字が出せるようになったことが、読み取り専用をやめた条件そのものである。
フッタが `vimeu-eval --user` を名指しするのは、**この窓には出せない情報がある**と窓自身が
言っておく必要があるからで、編集済みの組数も併記する。

**被害の上限は 1 セルである。** 1 回の編集が動かすのは行列の 1 マスだけで、他の 714 万マスは
Mozc の値のまま — `ConnectionTests.testAnEditTouchesExactlyOneCell` が固定している。
共起の窓（§3.5）と同じ発想で、誤った編集をしたときに何が起こりうるかを先に有界にしてある。

### 4.3 IMKit 上の制約（2026 指針より）

- `InputMethodConnectionName` は `<bundle id>_Connection` 厳守。違うとサンドボックス下で
  NSConnection の登録に失敗し、IME がまったく読み込まれない
- IMKit のヘッダは Swift Concurrency と相性が悪い。`IMKInputController` のメソッドは
  nonisolated として取り込まれ、override で隔離を足せない。`VimeuInputController` は
  nonisolated のままにし、MainActor の状態へは `mainSync` で同期的に渡る。
  `VimeuUI` 側は既定隔離を MainActor にする（`Package.swift` の `defaultIsolation`）
- `setMarkedText` と `insertText` はブロッキングで、**MainActor 上で同期的に**呼ぶ必要がある
- `IMKCandidates` は使わない。自前の `NSPanel`（候補）と `NSWindow`（調整）の **2 枚だけ**を
  遅延生成して使い回す。macOS 26 は NSWindow のメモリを回収しないため、開くたびに作らない
- **`LSBackgroundOnly` は設定しない**。通常時は `LSUIElement` だけにする。前者はより強い指定で、
  プロセスが前面に来ること自体を禁じるため、調整ウィンドウを前面に出せてもキー入力を
  受け取れない。加えて、このプロセスは自前のメニューバーを持たないので ⌘V などが
  フィールドエディタへ配送されない。`AdjustmentWindowController` がローカルイベントモニタで
  レスポンダチェーンへ手動転送している。調整ウィンドウを開いている間だけ
  `setActivationPolicy(.regular)` で通常アプリに切り替える。この間はバンドルのアイコンが
  Dock に現れ、Dock から調整ウィンドウを選択・再前面化できる。ウィンドウを閉じた時点で
  `.accessory` に戻し、入力メソッド単体では Dock を占有しない
- `activateServer` のたびに自プロセスのフットプリントを確認し、1024MB を超えたら通知して終了する
- **`UIDesignRequiresCompatibility` は設定しない**。指針は LiquidGlass のメモリ増を避ける
  手段としてこれを挙げているが、実体は **macOS 15 の描画パスへ戻すキー**で、全コントロールが
  旧デザインで描かれる。実測の削減量は起動直後の RSS で誤差であり、見た目を古くする対価に見合わない

---

## 5. 検証

「Mozc の辞書とコスト計算をそのまま使っている」ことと、「その上に足した唯一の項が
そこからはみ出していない」ことは、次の 7 段で機械的に確かめられる。

1. **辞書の一致** — `vimeu-dictbuild verify --dic dict/vimeu.dic --mozc-data <MOZC_DATA>` が、
   2,672 件の品詞名・2,672 件の boundary ペナルティ・接続行列の一様サンプル・単語トークンの
   サンプルを原本と突き合わせる。差分ゼロなら終了コード 0
2. **コスト計算の一致** — `ConverterTests` が Mozc の漸化式どおりの最小コスト経路になることを
   手計算値で固定し、`testNBestMatchesExhaustiveSearch` が A\* の出力を全経路列挙と突き合わせる。
   `cost == wordCost + connectionCost` の恒等式も固定
3. **変換精度** — `vimeu-eval` を 3 つのテストセットで実測（§3.6）。とくに Mozc 自身の回帰データ
   `corpus/mozc_evaluation.tsv` での文精度 91.14% が忠実度の直接の指標になる
4. **ユーザー辞書** — 合成規則・由来判定・クランプ・全トークンへの波及を単体テストで固定。加えて
   `ShippedDictionaryTests` が**実際の 52MB 辞書**に対して「隠す・上書き・追加」を通し、
   システム辞書を作り直しても編集が生き残ることも検証している
5. **共起** — `CollocationTests` が、自立語の判定・助詞を飛ばしたペアの取り方・正のペアの向き・
   負のペアの順序なしハード除外・ペア込みの恒等式・保存と再読み込みを固定する。とりわけ**窓の外へは届かないこと**
   （3453 以上負けている候補はペアがあっても繰り上がらない）を 1 本のテストで押さえてある —
   これが誤ったペアを登録したときの被害の上限そのものだからである。
   ペアが 0 件のときに 3 つのテストセットの数値が 1 桁も動かないことも実測で確認している
6. **接続の編集** — `ConnectionTests` が、合成規則・∓500 の刻み・クランプ・`disabled` の天井と
   復活・編集込みでの恒等式・保存と再読み込みを固定する。とりわけ次の 2 本が要である:
   **1 回の編集が動かすのは行列の 1 マスだけ**（`testAnEditTouchesExactlyOneCell`）と、
   **品詞 ID を振り直した辞書に載せ替えても編集が同じ品詞の組に付いていく**
   （`testEditIsKeyedByPOSNameNotID`）。前者が誤った編集の被害の上限、
   後者が Mozc のデータ更新に対する耐性である。
   大域的な影響は `vimeu-eval --user` で 3 テストセットに掛けて測る（§4.2 に実測値）
7. **サンドボックス下の動作** — インストール済みプロセスの `vmmap` に 52.3MB の `vimeu.dic` が
   `mapped file` として現れ、常駐は 176KB。プロセス全体の RSS は約 33.5MB

## 6. リポジトリ構成

```
vimeu/
├── DESIGN.md              本書（設計の正本）
├── Package.swift          依存ゼロ
├── Makefile               ビルド・辞書生成・インストール
├── Sources/
│   ├── VimeuDict/         .dic フォーマット（writer / mmap reader）
│   ├── VimeuUserDict/     ユーザー編集の値型と TSV ストア
│   ├── VimeuEngine/       ラティス + Viterbi + A* n-best + オーバレイ + 共起 + 編集 API
│   ├── VimeuInput/        ローマ字オートマトン
│   ├── VimeuUI/           候補パネル・調整ウィンドウ
│   ├── VimeuIME/          IMKInputController・エントリポイント
│   ├── vimeu-dictbuild/   辞書ビルド CLI
│   ├── vimeu-eval/        精度回帰 CLI
│   └── vimeu-preview/     調整ウィンドウを IME の外で開く開発用ハーネス
├── Tests/
├── Resources/             Info.plist / entitlements / アイコン
├── dict/                  辞書データ（生成物は gitignore）
└── corpus/                精度回帰用テストセット
```
